# ── Perfil prod: H2 en memoria ──────────────────────────────────────────────
# Render tiene filesystem efímero → no persiste H2 file.
# Datos se reinician en cada deploy (aceptable para demo/TFG).
spring.datasource.url=jdbc:h2:mem:coachlabdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
